Compilation of Solar Geometry library:

* On Unix, see INSTALL document.

* On Windows / MINGW, you should add the symbol _MINGW_.

* On Windows / Visual C++, you should add the symbol _VISUAL_.
